void arrange(long long arr[], int n) {
        for(int i=0;i<n;i++)
        {
            int k=arr[i]%n;
            arr[i]=((arr[k]%n)*n)+arr[i];
           // cout<<arr[i]<<"\n";
        }
        for(int i=0;i<n;i++)
        arr[i]=arr[i]/n;
        // Your code here
        
    }
