#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
 {
	int t;
	cin>>t;
	while(t--)
	{
	    int n,k;
	    cin>>n>>k;
	    int arr[n];
	    for(int i=0;i<n;i++)
	    {
	        cin>>arr[i];
	    }
	    priority_queue<int,vector<int>,greater<int>>pq;
	    int i=0,j=0;
	    while(i<n)
	    {
	        if(pq.size()<=k)
	        {
	        pq.push(arr[i]);
	        i++;
	        }
	        else
	        {
	            arr[j]=pq.top();
	            pq.pop();j++;
	        }
	    }
	    while(!pq.empty())
	    {
	        arr[j]=pq.top();
	        j++;pq.pop();
	    }
	        for(int i=0;i<n;i++)
	        cout<<arr[i]<<" ";
	        cout<<"\n";
	    
	}
	return 0;
}
